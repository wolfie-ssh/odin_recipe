#include <stdio.h>
int main(void) {
    double x=5.5, y=6.5;
    double add=x+y;
    double sub=x-y;
    double mult=x*y;
    double div=x/y;

    printf("Addition: %f\n", add);
    printf("Subtraction: %f\n", sub);
    printf("Subtraction: %f\n", mult);
    printf("Subtraction: %f\n", div);
    return 0;
}